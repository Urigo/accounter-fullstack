---
'@accounter/client': patch
---

Replace Mantine's `Paper`, `List`, `Text`, `Box`, `Collapse`, `Loader`, `Badge`, `Card`, `Grid` and
`Group` in the charges screens with `ui/card`, `ui/badge`, `ui/collapsible`, the house spinner and
Tailwind. Adds `animate-collapsible-down`/`-up` utilities for the collapse animation.
