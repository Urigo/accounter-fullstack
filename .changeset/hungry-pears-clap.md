---
'@accounter/client': patch
---

Replace Mantine's `Table`, `Paper`, `Text` and `Progress` across the reports screens with
`ui/table`, `ui/card`, Tailwind and a new `ui/segmented-progress` component. `reports/` no longer
imports Mantine.
