---
'@accounter/client': patch
---

- Replaces the flat tags list with a collapsible hierarchical tree view, search input, and
  edit/delete dialogs
- Adds new shared UI components: `Spinner`, `InputGroup`, `Field`, `Empty`, `AlertDialog`
- Updates the `addTag` mutation to support a `parentTag` parameter and fixes tag description display
  in combo-box/multi-select
