---
'@accounter/client': patch
---

Replace the last of Mantine's components outside the root layout: `Carousel` (on a new
`ui/carousel` built on embla), `Dropzone` (native drag-and-drop), `Drawer` (`PopUpDrawer`),
`Checkbox`, `Card`, `Table`, `Loader`, `Badge` and `Image`.
