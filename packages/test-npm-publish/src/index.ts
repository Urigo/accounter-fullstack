/**
 * Test package for debugging NPM publish workflow with OIDC
 */
export function hello(name: string): string {
  return `Hello, ${name}! This is a test package.`;
}

export const version = '0.0.0';
